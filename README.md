# FastAPI-Project
Python FastAPI Project

# Setup
1. Creating Virtual Python Environment `python3 -m venv ~/.venv` or `virtualenv ~/.venv`
2. Activate environment `source ~/.venv/bin/activate`
3. Creating Files
    -   `touch requirements.txt Dockerfile Makefile`
4. Populate the Makefile